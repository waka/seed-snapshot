default_connection: 'mysql2'

connections:
  mysql2:
    arunit:
      username: mysql
      password: root
      encoding: utf8
      collation: utf8_unicode_ci
    arunit2:
      username: mysql
      password: root
      encoding: utf8
